# 项目目录说明

近期对部分子目录名称进行了调整以便清晰表达含义，保留了符号链接保证旧路径仍可用：
- `A` → `cognitive_features`（符号链接：`A -> cognitive_features`）
- `B` → `activity_regularity`（符号链接：`B -> activity_regularity`）
- `C` → `survival_outcomes`（符号链接：`C -> survival_outcomes`）
- `just_test。。` → `quick_checks`（符号链接：`just_test。。 -> quick_checks`）
- `result_optmise` → `result_optimize`（符号链接：`result_optmise -> result_optimize`）

如有脚本仍引用旧路径，可继续工作；新代码建议直接使用新的目录名称。
